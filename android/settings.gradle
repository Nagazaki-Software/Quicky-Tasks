pluginManagement {
    def flutterSdkPath = {
        def properties = new Properties()
        file("local.properties").withInputStream { properties.load(it) }
        def flutterSdkPath = properties.getProperty("flutter.sdk")
        assert flutterSdkPath != null, "flutter.sdk not set in local.properties"
        return flutterSdkPath
    }()

    includeBuild("$flutterSdkPath/packages/flutter_tools/gradle")

    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    // Mantém os repositórios definidos aqui como fonte principal
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        // Repositório do Flutter para artefatos nativos de alguns plugins
        maven { url "https://storage.googleapis.com/download.flutter.io" }
    }
}

plugins {
    id "dev.flutter.flutter-plugin-loader" version "1.0.0"

    // Alinhado com Gradle 8.9 (veja o passo 2)
    id "com.android.application" version "8.7.3" apply false

    // Kotlin estável e compatível com AGP 8.7.x
    id "org.jetbrains.kotlin.android" version "2.0.21" apply false

    // Google Services atualizado (o 4.3.8 é velho e dá conflito)
    id "com.google.gms.google-services" version "4.4.2" apply false
}

include ":app"
