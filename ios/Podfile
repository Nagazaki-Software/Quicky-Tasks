# Fonte oficial dos specs
source 'https://cdn.cocoapods.org/'

platform :ios, '15.0'
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

# Descobre o FLUTTER_ROOT a partir do Generated.xcconfig
def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end
  File.foreach(generated_xcode_build_settings_path) do |line|
    m = line.match(/FLUTTER_ROOT\=(.*)/)
    return m[1].strip if m
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}."
end

# Helper do Flutter (use a versão com .rb)
require File.expand_path(File.join(flutter_root, 'packages', 'flutter_tools', 'bin', 'podhelper.rb'))

flutter_ios_podfile_setup

target 'Runner' do
  # Braintree e cia funcionam bem com linkage estática (menos treta de linkagem)
  use_frameworks! :linkage => :static

  # Plugins do Flutter
  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))

  # ===== Braintree via CocoaPods (NÃO via git) =====
  # Umbrella + subspecs corretos para CocoaPods (equivalentes aos módulos SPM)
  # Ajuste a faixa se quiser; 6.36 atende os requisitos atuais do SDK
  pod 'Braintree',               '~> 6.36'
  pod 'Braintree/Card',          '~> 6.36'
  pod 'Braintree/ThreeDSecure',  '~> 6.36'
  pod 'Braintree/DataCollector', '~> 6.36'
  # ================================================

end

target 'ImageNotification' do
  # Frameworks estáticos aqui também para consistência
  use_frameworks! :linkage => :static
  pod 'Firebase/Messaging'
end

post_install do |installer|
  # Ajustes padrão do Flutter (usa o objeto installer)
  flutter_additional_ios_build_settings(installer)

  # Garante target mínimo e Swift coerentes em todos os pods
  installer.pods_project.targets.each do |target|
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
      config.build_settings['SWIFT_VERSION'] = '5.0'
    end
  end
end
